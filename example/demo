#!/usr/bin/env node
const getopts = require("../")

console.log(
  getopts(process.argv.slice(2), {
    alias: {
      s: "super",
      b: "blitz"
    },
    default: {
      turbo: true
    }
  })
)
